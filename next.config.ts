/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Remove any experimental features or unnecessary options
  output: "standalone",
}

module.exports = nextConfig

